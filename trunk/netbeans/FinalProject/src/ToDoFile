1) Secondary Sorting encapsulated in the Int+ValType [DONE]
1a) HPhase1 modified [DONE]
1b) HPhase5 modified [DONE]

2) Need to adjust the serialization/deserialiazation & printing/parsing
  of the Matrix Vector with the new syntax (row/column number).
  ===>> SequenceOutputFormat can be used for intermedite data
  ===>> Adding the context variable k.

* Need to factorize all the phase to procede towards WPhase

* Distributed Cache instead of reading from distributed File System

Other Things:

* The C Phase4's parameter need to be invoked as a file name,
  instead of his directory

* All the test must be added (number of parameters control, etc)

* The X resulting matrix changes the effective size of the X should have Matrix:
	===> The problems arise when we are computing the Phase 5
	===> Unwanted Feature!!!
	(WE MUST TAKE CARE TO SEE WHAT HAPPENS IN THE FOLLOWING HPHASES)

Dubt:

* FileSplit vs InputSplit

